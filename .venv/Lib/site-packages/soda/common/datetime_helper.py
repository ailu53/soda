from datetime import datetime

from dateutil.parser import isoparse


def datetime_from_iso_zulu(date_string: str) -> datetime:
    """Soda Cloud sends timestamps in ISO 8601 format with Zulu time zone and sometimes larger precision than python can handle.
    This function converts the string to a python compatible datetime object."""
    return isoparse(date_string)
