from enum import Enum


class ScanType(Enum):
    CICD = "cicd"
    Agent = "agent"
    Custom = "custom"
    Discussion = "discussion"
    Remote = "remote"

    def __str__(self):
        return self.name
